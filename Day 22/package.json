{
    "name": "escrow-smartcontract",
    "version": "1.0.0",
    "description": "An advanced Escrow smart contract.",
    "scripts": {
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "deploy": "npx hardhat run scripts/deploy_escrow_smartcontract.js --network goerli"
    },
    "dependencies": {
        "@nomicfoundation/hardhat-toolbox": "^2.0.0",
        "chai": "^4.3.4",
        "ethers": "^5.7.0",
        "hardhat": "^2.17.0"
    }
}
