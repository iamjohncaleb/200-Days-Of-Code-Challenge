{
    "name": "anti-whale-token",
    "version": "1.0.0",
    "description": "Anti-whale ERC-20 style token with transaction limits",
    "scripts": {
      "test": "npx hardhat test",
      "deploy": "npx hardhat run scripts/deploy_awt.js --network localhost"
    },
    "dependencies": {
      "@nomicfoundation/hardhat-toolbox": "^2.0.0",
      "dotenv": "^16.3.1",
      "ethers": "^5.7.2",
      "hardhat": "^2.17.0"
    }
  }
  