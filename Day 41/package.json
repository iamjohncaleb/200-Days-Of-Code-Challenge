{
    "name": "secure-identity-verification",
    "version": "1.0.0",
    "description": "A Solidity smart contract for secure on-chain identity verification",
    "scripts": {
      "compile": "npx hardhat compile",
      "test": "npx hardhat test",
      "deploy": "npx hardhat run scripts/deploy_identity.js --network localhost"
    },
    "dependencies": {
      "ethers": "^6.8.1",
      "hardhat": "^2.20.0"
    },
    "devDependencies": {
      "@nomicfoundation/hardhat-toolbox": "^2.0.0"
    }
  }
  