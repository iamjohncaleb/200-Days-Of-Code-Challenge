{
    "name": "subscription-service",
    "version": "1.0.0",
    "description": "Smart contract for managing subscriptions with a fixed fee and duration",
    "scripts": {
      "test": "npx hardhat test",
      "deploy": "npx hardhat run scripts/deploy_subscription.js --network localhost"
    },
    "dependencies": {
      "@nomicfoundation/hardhat-toolbox": "^2.0.0",
      "dotenv": "^16.3.1",
      "ethers": "^5.7.2",
      "hardhat": "^2.17.0"
    }
  }
  