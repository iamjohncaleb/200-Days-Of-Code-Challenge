{
    "name": "border-control-token",
    "version": "1.0.0",
    "description": "Border Control Permit & Visa Tokenization Smart Contract",
    "scripts": {
      "test": "npx hardhat test",
      "compile": "npx hardhat compile",
      "deploy": "npx hardhat run scripts/deploy.js --network localhost"
    },
    "devDependencies": {
      "chai": "^4.3.7",
      "ethereum-waffle": "^3.4.4",
      "ethers": "^6.6.0",
      "hardhat": "^2.22.0"
    }
  }
  