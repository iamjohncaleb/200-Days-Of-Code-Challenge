{
    "name": "multi-sig-wallet",
    "version": "1.0.0",
    "description": "Smart contract for a multi-signature wallet with configurable owners and required approvals.",
    "scripts": {
      "test": "npx hardhat test",
      "deploy": "npx hardhat run scripts/deploy_multi_sig_wallet.js --network localhost"
    },
    "dependencies": {
      "@nomicfoundation/hardhat-toolbox": "^2.0.0",
      "dotenv": "^16.3.1",
      "ethers": "^5.7.2",
      "hardhat": "^2.17.0"
    }
  }
  