{
    "name": "warfund-crowdfunding",
    "version": "1.0.0",
    "description": "A crowdfunding smart contract for war funds.",
    "scripts": {
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "deploy": "npx hardhat run scripts/deploy_warfund_crowdfunding.js --network goerli"
    },
    "dependencies": {
        "@nomicfoundation/hardhat-toolbox": "^2.0.0",
        "chai": "^4.3.4",
        "ethers": "^5.7.0",
        "hardhat": "^2.17.0"
    }
}